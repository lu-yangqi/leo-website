# 全局 AGENTS.md

## 工具与 Shell 使用

### 总则

* **统一使用 PowerShell 7 作为 Shell 环境。** 普通 CLI、脚本和 Windows 原生操作均使用 `pwsh`，非必要不要使用旧版 `powershell.exe` 或 `cmd.exe`，也不要混用 Bash 与 PowerShell 语法。
* **FastCtx 是 PowerShell 工作流中的优先工具。** FastCtx 已提供专用能力的操作优先使用 FastCtx；未覆盖的操作直接使用 PowerShell 7。

### FastCtx 规范

* **文件读取、搜索和发现：** 分别使用 `inspect_local_file`、`grep` 和 `glob`，不要用 `Get-Content`、`Select-String`、递归 `Get-ChildItem` 等命令完成等价工作。
* **读取应按需且尽量批量。** 只读取当前任务需要的内容；需要多个文件时，通过一次 `inspect_local_file` 批量传入。结果为 `Partial` 时，仅按照返回提示提供的参数继续读取。
* **确定性的跨文件机械替换：** 使用 `replace`，写入前先执行 `dry_run`。需要理解代码语义的修改、生成式重写或小范围局部编辑使用 `apply_patch`。
* **非交互式长后台任务：** 构建、测试、开发服务器等优先使用 `run_background`，并通过 `job_output` 获取结果。需要用户输入、密码提示或全屏控制台交互的程序，不能作为后台任务执行。

### PowerShell 规范

* **保证执行可靠性。** PowerShell 脚本首部设置 `$ErrorActionPreference = 'Stop'`，遇到错误立即中断，避免静默失败。复杂命令应拆分为更简单、可独立验证的步骤，避免不必要的多层 Shell 嵌套。
* **优先使用结构化数据处理。** 文件生成和读写需要指定编码时显式使用 UTF-8；数据解析和转换优先使用 PowerShell 对象管道，如 `Select-Object`、`Where-Object`，避免不必要的文本截取。
* **严格遵循 PowerShell 的引号与转义规则。** 不要混用 Bash 风格的转义语法，也不要在 PowerShell 字符串中使用 `\"` 转义双引号。外层已经是 PowerShell 时，传给 `-Command` 的脚本优先使用单引号包裹，避免 `$变量` 被提前展开；确需跨层传递变量时，应确保 `$` 在正确的 Shell 层级展开。
* **Shell 敏感内容优先使用单引号保护。** `rg` 等 CLI 的正则或参数包含 `|`、`(`、`)`、`\`、`$` 等可能被 PowerShell 解释的字符时，优先使用单引号包裹；若命令因此需要多层复杂转义，应拆分命令，而不是继续堆叠引号和转义。


## 子代理使用

**对于简单、可直接完成的任务，或用户明确要求不使用子代理的任务，跳过本节。**

子代理用于**探索、检索与独立核验**，是处理「宽而重」读取任务的工具。仅当它能减少主线程上下文污染、提高并行度或提供独立验证时使用；需要时可在工作的任何阶段派发，而不局限于任务开始。主代理负责对子代理进行编排，并承担方案取舍、代码修改和最终验证。

### 何时直接处理

以下内容由主代理直接读取和处理：

* 已知位置的小文件、少量代码或单一事实；
* 即将修改的具体代码；
* 委派、等待和复核成本不低于直接处理的任务；
* 奠基性文档，如架构文档、设计文档、交接备忘录等。此类文档用于建立全局视角，其价值依赖完整细节与上下文，不能因篇幅较长而外包。

### 何时适合派发

适合交给子代理的任务包括：

* 巨型文件（奠基性文档除外）、跨文件或跨目录检索；
* 相互独立、可并行的探索或核验；
* 长任务中对模块现状的重新确认；
* 会产生大量日志、搜索结果或外围材料的阅读。

多个独立任务应尽量并发，并拆成轻量、范围明确的子任务。

### 委派与验证

委派任务必须**自包含**，明确说明检索范围、具体问题和期望输出。精度重要时，要求返回 `file:line`、符号名及必要的关键原文，作为主代理后续复核的依据。

子代理结果只是经过压缩的线索，可能遗漏或出错。复核应沿其提供的出处进行针对性抽查，而不是重新通读其已经处理的材料，否则会失去委派的意义。只有结论重要、可疑或需要直接依据时，才回到对应原文点验。

主代理必须亲自完整阅读的只有两类内容：

1. 即将修改的确切代码；
2. 奠基性文档。

对子代理而言，这两类内容至多负责定位，不负责代替主代理阅读。

子代理只负责探索、检索和核验；代码修改、方案取舍及最终验证始终由主代理负责。

### 派发机制

* 是否派发及派发数量由主代理自主决定，无需用户明确要求；较重探索优先拆成多个轻量任务并发处理。
* 最多并行 10 个子代理。10 是上限而非目标；根据实际任务决定数量，倾向于更多并行、单个任务更轻更聚焦。
* 派生时除 `fork_turns` 外省略所有可选参数，不传 `agent_type`、`model`、`reasoning_effort`、`service_tier`，由泛型派生加载 `default.toml`；不得选择 `worker`、`explorer` 或其他角色。
* 必须显式设置 `fork_turns = "none"`，不继承主代理历史。因此，每个子代理的任务描述必须自包含。
* 每个子代理只使用一轮：不复用、不追派、不使用 `followup_task`。需要进一步探索时，重新派发一个干净的新子代理。

### 等待与介入

* 派发后立即进入 `wait_agent`。只要仍有会影响当前任务的子代理运行，就继续等待，不自行推进其已委派范围内的工作。委派后，主代理的职责转为编排、等待和收敛结果。
* 每次 `wait_agent` 返回只表示发生了一个事件，可能是完成，也可能是超时。返回后处理已有终态结果、检查仍运行的代理；仍需等待则继续调用 `wait_agent`。通过信封中的 Sender（代理路径）识别子代理，不使用 Task name。
* `MESSAGE` 仅是过程信号；只有 `FINAL_ANSWER` 或 completed 状态属于终态。
* 子代理运行期间，不修改其检索范围内的文件；涉及全仓检索、否定结论或跨目录关系时，冻结整个工作区的修改。
* 子代理明显滞后时，可用 `send_message` 催收一次，要求停止扩大范围并尽快以 partial 状态返回已核实内容；仍无终态则使用 `interrupt_agent`。若缺口仍重要，缩小范围后重新派发，不原样重试。
* `send_message` 只用于催收或收紧范围，不用于追加问题、改变任务或来回追问；需要实质改变任务时，中断后重新派发。


## 软件工程原则

**对于非软件工程任务，跳过本节，除非用户指令明确要求。**

* **优先采用能够完整满足当前需求的最简单实现。** 优先局部修改、复用现有代码和项目已有依赖；成熟可靠的第三方库能降低整体复杂度时，应优先使用。不要为假设的未来需求预建抽象、配置或其他间接机制，也不要重复造轮子。避免无语义的硬编码，对具有业务含义、需要复用或可能调整的值使用有意义的命名常量。简单性不得以牺牲正确性、必要的错误处理、安全边界、可读性或可测试性为代价；架构和依赖选择应考虑长期维护，非必要不要采用明确准备日后替换的临时方案。

* **不要保留向后兼容性。** 除非明确要求，对已经废弃的实现直接删除，而不是增加兼容层、fallback（回退机制）或 migration（迁移机制）。

* **不要默默假设未明确的细节。** 若存在会实质影响实现方式的歧义，首先查询上下文、项目文档等以明确需求；仍无法可靠判断时，先询问再实现。

* **保持架构清晰、模块化且可逐步扩展，并遵循最小惊讶原则。** 明确划分组件职责，使 API、代码行为和功能设计符合其命名、上下文以及开发者和用户的合理预期，避免隐藏副作用和反直觉行为；不要为了尚未完成的复杂设计牺牲当前可工作的系统。
